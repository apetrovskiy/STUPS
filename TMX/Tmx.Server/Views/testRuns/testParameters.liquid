<html>
    <head>
        <title>Test parameters</title>
        <meta http-equiv="refresh" content="30">
    </head>
    <body>
        <a href="/">Home</a>
        <br>
        <a href="/testRuns/testRuns">Test runs</a>
        <br>
        <br>
        
        <h1>{{ @Model.TestRun.Name }} /{{ @Model.TestRun.Status }}/</h1>

        {% for item in @Model.TestRun.Data.Data -%}
            ['{{ item.Key }}'='{{ item.Value }}']<br>
        {% endfor -%}
        
    </body>
</html>